'use strict'

/**
 * 
 * Let y Const en JavaScript
 * 
 * Antes de ES6, JavaScript usaba la palabra clave var que solo usaba función y alcance global. No había un alcance a nivel de bloque.

Con la adición de let y const, JavaScript agregó alcance de bloque.

Let - Cuando declaramos una variable usando la palabra clave let, podemos asignar un nuevo valor a esa variable más tarde, pero no podemos volver a declararla con el mismo nombre.

const - Cuando declaramos una variable como constante, no podemos volver a definir o volver a declarar otra variable constante con el mismo nombre en el mismo ámbito (función o ámbito de bloque). Pero podemos cambiar los valores almacenados en esa variable si la variable es de un tipo de referencia como una matriz u objeto.
 */
